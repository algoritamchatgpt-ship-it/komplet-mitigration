--- DOLE
    PUSH KEY CLEAR
    SELECT OS0
    IF.NOT.EOF()
    SKIP
    ENDIF
    THISFORM.GRD0.SETFOCUS
    THISFORM.REFRESH
    POP KEY
    ENDPROC
--- GORE
    PUSH KEY CLEAR
    SELECT OS0
    IF.NOT.BoF()
    SKIP-1
    ENDIF
    THISFORM.GRD0.SETFOCUS
    THISFORM.REFRESH
    POP KEY
    ENDPROC
--- ZADNJI
    PUSH KEY CLEAR
    SELECT OS0
    IF.NOT.EOF()
    GO BOTTOM
    ENDIF
    THISFORM.GRD0.SETFOCUS
    THISFORM.REFRESH
    POP KEY
    ENDPROC
--- PRVI
    PUSH KEY CLEAR
    SELECT OS0
    IF.NOT.BoF()
    GO TOP
    ENDIF
    THISFORM.GRD0.SETFOCUS
    THISFORM.REFRESH
    POP KEY
    ENDPROC
--- \<DODAJ
    DO OS0DODAJ
    ENDPROC
--- IZLAZ
    RELEASE THISFORM
    ENDPROC
--- \<KARTICA
    PUSH KEY CLEAR
    SELECT OS0
    DO  FORM OS0K
    THISFORM.GRD0.SETFOCUS
    THISFORM.Refresh
    POP KEY
    ENDPROC
--- POPISNA LISTA
    PUSH KEY CLEAR
    DO  FORM OSPOPIS
    POP KEY
    ENDPROC
--- GRUPE ZA AMORTIZACIJU
    PUSH KEY CLEAR
    SELECT OS0
    DO  FORM OSAG
    SELECT OSAG
    SET ORDER TO 1
    SELECT OS0
    THISFORM.REFRESH
    POP KEY
    ENDPROC
--- PODGRUPE ZA AMORTIZACIJU
    PUSH KEY CLEAR
    SELECT OS0
    DO  FORM OSAGPOD
    SELECT OSAGPOD
    SET ORDER TO 1
    SELECT OS0
    THISFORM.REFRESH
    POP KEY
    ENDPROC
--- \<NEPOKRETNOSTI
    PUSH KEY CLEAR
    SELECT OS0
    MOSIFRA=OSIFRA
    mfile='OS0NEPOKR'
    uslov="OSIFRA=MOSIFRA"
    DO printer_bullzip WITH mdata02,mfile,uslov
    THISFORM.GRD0.SETFOCUS
    THISFORM.Refresh
    POP KEY
    ENDPROC
--- SORT
    push key clear
    SELECT OS0
    MORDER=TAGNO()
    DO CASE MORDER
    CASE MORDER=0
    SET ORDER TO 1
    CASE MORDER=1
    SET ORDER TO 2
    CASE MORDER=2
    SET ORDER TO 3
    CASE MORDER=3
    SET ORDER TO 4
    CASE MORDER=4
    SET ORDER TO 5
    CASE MORDER=5
    SET ORDER TO 6
    CASE MORDER=6
    SET ORDER TO 7
    CASE MORDER=7
    SET ORDER TO 8
    CASE MORDER=8
    SET ORDER TO 0
    OTHERWISE
    SET ORDER TO
    ENDCASE
    THISFORM.GRD0.SETFOCUS
    THISFORM.REFRESH
    POP KEY
    ENDPROC
--- BRISANJE PRAZNINA
    PUSH KEY CLEAR
    SELECT OS0
    USE
    USE OS0 IN 0 EXCLUSIVE
    SELECT OS0
    DELETE ALL FOR NAZ=SPACE(40)
    PACK
    THISFORM.ReLEASE
    KEYBOARD '{ENTER}'
    POP KEY
    ENDPROC
--- IZVOZ TXT-1
    PUSH KEY CLEAR
    MMIME='POPIS_IMOVINE.TXT'
    FIDAT=FCREATE(MMIME)
    MTARIFA='1'
    DO WHILE.NOT.EOF()
    IF naz<>SPACE(40)
    MM='20'+';'+ALLTRIM(OSIFRA)+ALLTRIM(NAZ)+ALLTRIM(STR(GODPRO,4,0))+';'+ALLTRIM(SUBSTR(KONTO,1,6))+';'+ALLTRIM(VRSTA)+';';
    +ALLTRIM(STR(MTR,4,0))+';'+ALLTRIM(SUBSTR(INVBROJ,4,0))+';'+ALLTRIM(STR(STOPAOT,10,2))+';'+ALLTRIM(DTOC(DATNAB))+';'+;
    ALLTRIM(STR(NAB0,18,2))+';'+ALLTRIM(STR(SAD0,18,2))+';'
    FPUTS(fidat,MM)
    endif
    SKIP
    ENDDO
    GO top
    FCLOSE(fidat)
    MMIME='RADNE_JEDINICE_2012.TXT'
    FIDAT=FCREATE(MMIME)
    MM=VRSTA+';'+'NAZIV RADNE JEDINICE                              '
    FPUTS(fidat,MM)
    FCLOSE(fidat)
    MMIME='RACUNOPOLAGAC_2012.TXT'
    FIDAT=FCREATE(MMIME)
    MM=VRSTA+';'+'IME RACUNOPOLAGACA                                '
    FPUTS(fidat,MM)
    GO top
    FCLOSE(fidat)
    POP KEY
    ENDPROC
--- IZVOZ EXCEL
    PUSH KEY CLEAR
    COPY TO OS0 TYPE XLS FOR naz<>SPACE(40)
    POP KEY
    ENDPROC
--- IZVOZ EXCEL 2
    PUSH KEY CLEAR
    COPY TO OS0 TYPE XLS FOR naz<>SPACE(40) FIELDS osifra,naz,godpro,konto,vrsta,mtr,invbroj,stopaot,datnab,nab0,isp0,sad0,procena,razlproc
    POP KEY
    ENDPROC