=== form | OSPRENOSZUP | parent=(root) CAPTION=PRENOS ZADNJEG STANJA U POČETNO
--- BTN: POTVRDJUJEM
    PUSH KEY CLEAR
    IF THISFORM.TEXT1.VALUE='D'
    CLOSE ALL TABLES
    USE OS0 IN 0 ORDER 1
    SELECT OS0
    USE OS IN 0 EXCLU
    SELECT OS
    GO TOP
    DO WHILE.NOT.EOF()
    MNAB=NAB
    MISP=ISP
    MSAD=SAD
    MNAB2=NAB2
    MSAD2=SAD2
    MISP2=ISP2
    MOSIFRA=OSIFRA
    SELECT OS0
    SEEK MOSIFRA
    IF FOUND()
    REPLACE NAB0 WITH MNAB
    REPLACE SAD0 WITH MSAD
    REPLACE ISP0 WITH MISP
    REPLACE NAB02 WITH MNAB2
    REPLACE SAD02 WITH MSAD2
    REPLACE ISP02 WITH MISP2
    ENDIF
    SELECT OS
    SKIP
    ENDDO
    ENDIF
    SELECT OS0
    replace ALL iznosulag WITH 0
    replace ALL datulag WITH CTOD(' /  /    ')
    DO IDEL WITH (KKOCKAX)
    SET EXACT ON
    SELECT OS0
    USE
    SELECT OS
    USE
    RELEASE THISFORM
    POP KEY
    ENDPROC
--- BTN: ODUSTAJEM
    RELEASE THISFORM
    ENDPROC