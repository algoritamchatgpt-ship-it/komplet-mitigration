<Window x:Class="Algoritam.WPF.Views.Zarade.PregledKoloneView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pregled kolone"
        Width="980" Height="700"
        WindowStartupLocation="CenterOwner"
        Background="#F5F7FA">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#00695C" Padding="12,8" CornerRadius="4">
            <DockPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Kolona:"
                               VerticalAlignment="Center"
                               Foreground="White"
                               FontFamily="Tahoma"
                               FontSize="12"
                               Margin="0,0,8,0"/>
                    <ComboBox x:Name="KolonaCombo"
                              Width="260"
                              Height="28"
                              SelectionChanged="KolonaComboSelectionChanged"/>
                    <Button Margin="10,0,0,0"
                            Width="80"
                            Height="28"
                            Click="UcitajClick"
                            Background="#607D8B"
                            Foreground="White"
                            BorderThickness="0"
                            FontFamily="Tahoma"
                            FontWeight="Bold"
                            Content="UCITAJ"/>
                </StackPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button Width="90"
                            Height="28"
                            Margin="0,0,8,0"
                            Click="StampajClick"
                            Background="#2E7D32"
                            Foreground="White"
                            BorderThickness="0"
                            FontFamily="Tahoma"
                            FontWeight="Bold"
                            Content="STAMPAJ"/>
                    <Button Width="90"
                            Height="28"
                            Click="ZatvoriClick"
                            Background="#C62828"
                            Foreground="White"
                            BorderThickness="0"
                            FontFamily="Tahoma"
                            FontWeight="Bold"
                            Content="ZATVORI"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Border x:Name="PrintRoot"
                Grid.Row="1"
                Margin="0,10,0,10"
                Background="White"
                BorderBrush="#C8E6C9"
                BorderThickness="1"
                Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="NaslovKoloneText"
                               Grid.Column="0"
                               FontFamily="Tahoma"
                               FontSize="14"
                               FontWeight="Bold"
                               Foreground="#004D40"/>
                    <TextBlock Grid.Column="1"
                               Text="Ukupno:"
                               FontFamily="Tahoma"
                               FontSize="12"
                               FontWeight="Bold"
                               Foreground="#37474F"/>
                    <TextBlock x:Name="UkupnoText"
                               Grid.Column="3"
                               FontFamily="Tahoma"
                               FontSize="12"
                               FontWeight="Bold"
                               Foreground="#1565C0"/>
                </Grid>

                <DataGrid x:Name="GridRezultati"
                          Grid.Row="1"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          HeadersVisibility="Column"
                          RowHeaderWidth="0"
                          GridLinesVisibility="Horizontal"
                          AlternatingRowBackground="#E8F5E9"
                          FontFamily="Tahoma"
                          FontSize="12">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Br." Binding="{Binding Broj}" Width="90"/>
                        <DataGridTextColumn Header="Ime i prezime" Binding="{Binding ImePrez}" Width="*"/>
                        <DataGridTextColumn Header="Vrednost" Binding="{Binding Vrednost, StringFormat=N2}" Width="180"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="#F0F0F0" BorderBrush="#C8E6C9" BorderThickness="1" Padding="10,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="PREGL.KOL."
                           FontFamily="Tahoma" FontSize="9" Foreground="#C8E6C9"
                           VerticalAlignment="Center" Margin="0,0,14,0"/>
                <TextBlock Grid.Column="1" x:Name="StatusText"
                           FontFamily="Tahoma" FontSize="11"
                           Foreground="#37474F" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>
