<Window x:Class="Algoritam.WPF.Views.Zarade.LdKnjizenjeZaradaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Naslov}"
        Width="1260" Height="760"
        MinWidth="900" MinHeight="500"
        WindowStartupLocation="CenterScreen"
        Background="#E0F2F1"
        FontFamily="Tahoma" FontSize="11">

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Naslov -->
        <Border Grid.Row="0" Background="#00695C" Padding="10,7" CornerRadius="3" Margin="0,0,0,8">
            <TextBlock Text="KNJIŽENJE ZARADA — Šifarnik konta"
                       FontWeight="Bold" FontSize="13" Foreground="White"/>
        </Border>

        <!-- DataGrid -->
        <DataGrid x:Name="KnjizenjeGrid"
                  Grid.Row="1" Margin="0,0,0,8"
                  ItemsSource="{Binding Stavke}" SelectedItem="{Binding Selektovana}"
                  CurrentCellChanged="KnjizenjeGridCurrentCellChanged"
                  AutoGenerateColumns="False" IsReadOnly="False"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#B2DFDB" RowHeaderWidth="0"
                  Background="White" AlternatingRowBackground="#E0F2F1"
                  BorderBrush="#4DB6AC" BorderThickness="1">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#00695C"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="6,4"/>
                    <Setter Property="BorderBrush" Value="#004D40"/>
                    <Setter Property="BorderThickness" Value="0,0,1,0"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Vrsta"  Binding="{Binding Vrsta,  UpdateSourceTrigger=PropertyChanged}" Width="70"/>
                <DataGridTextColumn Header="Kod"    Binding="{Binding Kod,    UpdateSourceTrigger=PropertyChanged}" Width="100"/>
                <DataGridTextColumn Header="Opis"   Binding="{Binding Opis,   UpdateSourceTrigger=PropertyChanged}" Width="260"/>
                <DataGridTextColumn Header="Konto"  Binding="{Binding Konto,  UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                <DataGridTextColumn Header="Kontop" Binding="{Binding Kontop, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                <DataGridTextColumn Header="Iznos"  Binding="{Binding Iznos,  StringFormat=N2, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                <DataGridTextColumn Header="Datdok" Binding="{Binding Datdok, StringFormat=dd.MM.yyyy, UpdateSourceTrigger=PropertyChanged}" Width="110"/>
                <DataGridTextColumn Header="Brnal"  Binding="{Binding Brnal,  UpdateSourceTrigger=PropertyChanged}" Width="110"/>
                <DataGridTextColumn Header="Mp"     Binding="{Binding Mp,     UpdateSourceTrigger=PropertyChanged}" Width="70"/>
                <DataGridTextColumn Header="Mtr"    Binding="{Binding Mtr,    UpdateSourceTrigger=PropertyChanged}" Width="70"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Dugmad -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,6">
            <Border BorderBrush="#4DB6AC" BorderThickness="1" CornerRadius="3" Margin="0,0,8,0" Padding="6,4">
                <StackPanel>
                    <TextBlock Text="PRENOS PODATAKA" FontSize="9" FontWeight="SemiBold" Foreground="#00695C" Margin="0,0,0,3"/>
                    <StackPanel Orientation="Horizontal">
                        <Button Width="116" Height="28" Margin="0,0,4,0" Content="PRENOS KONTA"                Style="{StaticResource PrimaryButtonStyle}" Command="{Binding PrenosKontaCommand}"/>
                        <Button Width="220" Height="28" Margin="0,0,4,0" Content="PRENOS PODATAKA AKONTACIJA"   Style="{StaticResource PrimaryButtonStyle}" Command="{Binding PrenosPodatakaAkontacijaCommand}"/>
                        <Button Width="236" Height="28"                  Content="PRENOS PODATAKA KONAČNA ISPLATA" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding PrenosPodatakaKonacnaIsplataCommand}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
            <Border BorderBrush="#4DB6AC" BorderThickness="1" CornerRadius="3" Margin="0,0,8,0" Padding="6,4">
                <StackPanel>
                    <TextBlock Text="KNJIŽENJE" FontSize="9" FontWeight="SemiBold" Foreground="#00695C" Margin="0,0,0,3"/>
                    <StackPanel Orientation="Horizontal">
                        <Button Width="210" Height="28" Margin="0,0,4,0" Content="KNJIŽENJE UKALKULISANE ZARADE" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding KnjizenjeUkalkulisaneCommand}"/>
                        <Button Width="200" Height="28"                  Content="KNJIŽENJE ISPLATE ZARADE"      Style="{StaticResource PrimaryButtonStyle}" Command="{Binding KnjizenjeIsplateCommand}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
            <Border BorderBrush="#4DB6AC" BorderThickness="1" CornerRadius="3" Margin="0,0,8,0" Padding="6,4">
                <StackPanel>
                    <TextBlock Text="BRISANJE" FontSize="9" FontWeight="SemiBold" Foreground="#00695C" Margin="0,0,0,3"/>
                    <Button Width="96" Height="28" Content="BRIŠI SVE" Style="{StaticResource DangerButtonStyle}" Command="{Binding BrisiSveCommand}"/>
                </StackPanel>
            </Border>
            <Button Width="90" Height="28" VerticalAlignment="Bottom" Content="IZLAZ"
                    Click="ZatvoriClick" Style="{StaticResource NeutralButtonStyle}"/>
        </StackPanel>

        <!-- Status bar -->
        <DockPanel Grid.Row="3" Margin="0,2,0,0">
            <TextBlock DockPanel.Dock="Left" Text="KNJIŽENJE ZARADA" FontSize="9" Foreground="#4DB6AC" VerticalAlignment="Center" Margin="0,0,8,0"/>
        </DockPanel>
    </Grid>
</Window>
